<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiangshangban.device.dao.IUserDao">

    <!--请假表单-->
    <resultMap id="LeaveInfo" type="com.xiangshangban.activiti.bean.LeaveInfo">
        <result column="proposer_id" property="proposerId"/>
    </resultMap>

    <insert id="saveLeaveInfo" parameterType="com.xiangshangban.activiti.bean.LeaveInfo">
        INSERT INTO leave_info VALUES(#{proposerId},#{proposerName},#{processInstanceId},#{firstAssigneeId},
        #{secondAssigneeId},#{thirdAssigneeId},#{fourthAssigneeId},#{fifthAssigneeId},#{startTime},
        #{endTime},#{effectiveTimeLength},#{unit},#{reason},#{applicationStatus},#{applicationType},#{leaveType},#{createTime})
    </insert>

</mapper>